# apache-jira-llm-dataset
A scalable and fault-tolerant data pipeline that scrapes public issue data from Apache Jira projects and transforms it into clean, structured JSONL format for Large Language Model (LLM) training.
